export class LinkedListNode {
    /**
     * Creates a new node for a Linked List.
     * @param value Value of node.
     */
    constructor(value) {
        this.value = value;
        this.next = undefined;
    }
}
